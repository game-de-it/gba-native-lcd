# v0.1.0 release staging

`RetroArch-GBA-LCD-v0.1.0-kpa-arm64-development.apk` is the device-verified,
development-signed candidate. It is intentionally ignored by Git and must be
replaced by a persistent release-key build before publication.

Verify any candidate against `SHA256SUMS` before installation.
